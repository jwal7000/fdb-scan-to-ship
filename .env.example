# ── Read credentials (FDB production DB, read-only) ───────────────────────────
MYSQL_HOST=104.197.166.101
MYSQL_USER=FDB_SteveConnect
MYSQL_PASSWORD=your_read_password_here
MYSQL_DB=production

# ── Write credentials (Jesse needs to create this user) ───────────────────────
# Until these are set, scan writes go to local SQLite (scans_local.sqlite)
MYSQL_WRITE_USER=
MYSQL_WRITE_PASSWORD=
MYSQL_WRITE_HOST=104.197.166.101
MYSQL_WRITE_DB=production

# ── App config ─────────────────────────────────────────────────────────────────
PORT=3000
